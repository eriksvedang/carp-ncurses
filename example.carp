(load "ncurses.carp")
(use NCurses)

(defn main []
  (do (initscr)
      (start-color)
      (init-pair 1 COLOR_BLACK COLOR_RED)
      (attron (color-pair 1))
      (let-do [w (width)
               h (height)
               s (str* "Hello! " w ", " h)]
        (move (/ h 2) (- (/ w 2)
                         (/ (count &s) 2)))
        (printw s))
      (refresh)
      (getch)
      (endwin)))
